This project uses awilix for dependency injection : 

- Joi for request validation
- Run npm install
- Run npm run start

Create local.json in src/env folder and paster following content.

{
    "environment": "local",
    "port": 5000,
}
